---
phase: 05-sanity-cms-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/blog/[slug].astro
  - src/pages/blog/index.astro
  - src/pages/blog/[...page].astro
  - src/pages/blog/[...id].astro
  - src/layouts/BlogLayout.astro
  - src/components/blocks/blog/PostCards.astro
  - src/components/blocks/blog/PortableTextRenderer.astro
  - src/components/seo/BlogPostingSchema.astro
  - src/content.config.ts
  - src/content/blog/
autonomous: true

must_haves:
  truths:
    - "Blog listing page at /blog shows posts fetched from Sanity"
    - "Individual blog post at /blog/{slug} renders Portable Text content from Sanity"
    - "BlogPostingSchema renders valid JSON-LD with Sanity data"
    - "Portfolio collection still works (not broken by blog removal)"
    - "Build succeeds with astro build"
  artifacts:
    - path: "src/pages/blog/[slug].astro"
      provides: "Individual blog post page fetching from Sanity"
      contains: "getPostBySlugQuery"
    - path: "src/pages/blog/index.astro"
      provides: "Blog listing page fetching from Sanity"
      contains: "getAllPostsQuery"
    - path: "src/components/blocks/blog/PortableTextRenderer.astro"
      provides: "Portable Text to HTML rendering"
      contains: "PortableText"
    - path: "src/content.config.ts"
      provides: "Content collections with blog removed, portfolio kept"
      contains: "portfolio"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/lib/sanity/queries.ts"
      via: "GROQ query import"
      pattern: "getAllPostsQuery"
    - from: "src/pages/blog/[slug].astro"
      to: "src/lib/sanity/queries.ts"
      via: "GROQ query import"
      pattern: "getPostBySlugQuery"
    - from: "src/pages/blog/[slug].astro"
      to: "src/components/blocks/blog/PortableTextRenderer.astro"
      via: "component import for content rendering"
      pattern: "PortableTextRenderer"
    - from: "src/components/seo/BlogPostingSchema.astro"
      to: "src/pages/blog/[slug].astro"
      via: "props from Sanity data"
      pattern: "seoDescription"
---

<objective>
Rewrite blog pages to fetch from Sanity instead of markdown content collections. Create Portable Text renderer. Update BlogPostingSchema to work with Sanity data. Remove markdown blog infrastructure while preserving portfolio collection.

Purpose: This is the core integration -- making blog content come from Sanity CMS instead of local markdown files (CMS-03).
Output: Working blog listing and post pages powered by Sanity.
</objective>

<execution_context>
@/Users/thuijsmans/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thuijsmans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sanity-cms-integration/05-RESEARCH.md
@.planning/phases/05-sanity-cms-integration/05-CONTEXT.md
@.planning/phases/05-sanity-cms-integration/05-01-SUMMARY.md
@src/pages/blog/[...page].astro
@src/pages/blog/[...id].astro
@src/layouts/BlogLayout.astro
@src/components/blocks/blog/PostCards.astro
@src/components/ui/cards/BlogCard.astro
@src/components/seo/BlogPostingSchema.astro
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite blog listing and post pages for Sanity</name>
  <files>
    src/pages/blog/index.astro
    src/pages/blog/[slug].astro
    src/pages/blog/[...page].astro
    src/pages/blog/[...id].astro
    src/components/blocks/blog/PortableTextRenderer.astro
    src/components/blocks/blog/PostCards.astro
    src/layouts/BlogLayout.astro
  </files>
  <action>
    1. Create `src/components/blocks/blog/PortableTextRenderer.astro`:
       - Import `PortableText` from `astro-portabletext`
       - Create component that accepts `content` prop (Portable Text array)
       - Render with PortableText component
       - Wrap in the same styled container as current BlogLayout uses:
         `<div class="relative mx-auto max-w-5xl bg-white text-xl leading-9 dark:bg-neutral-950 [&_p]:px-8 [&_p]:first:pt-6 [&_hr]:hidden">`
       - Add DecorationBorder type="top bottom" style="solid" inside the wrapper (matching current layout)
       - Apply basic heading styles inline: h2 gets `px-8 pt-8 pb-2`, h3 gets `px-8 pt-6 pb-2`, blockquote gets `px-8 italic border-l-4 border-neutral-300`
       - Links in Portable Text should render as `<a>` with appropriate styling

    2. Create `src/pages/blog/index.astro` (REPLACE existing [...page].astro content):
       - Import sanityClient from `../../lib/sanity/client`
       - Import getAllPostsQuery from `../../lib/sanity/queries`
       - Import urlFor from `../../lib/sanity/image`
       - Fetch all posts: `const posts = await sanityClient.fetch(getAllPostsQuery)`
       - Transform Sanity data to match PostCards expected format:
         ```ts
         const transformedPosts = posts.map((post: any) => ({
           id: post.slug.current,
           data: {
             title: post.title,
             pubDate: post.pubDate,
             author: post.author,
             description: post.seoDescription || '',
             thumbnail: post.featuredImage ? urlFor(post.featuredImage).width(800).url() : '',
           }
         }))
         ```
       - Keep Layout, Hero, and PostCards components exactly as current [...page].astro uses them
       - PostCards basePath stays as '/blog'
       - Sort posts by pubDate descending (already sorted by GROQ query, but keep sort in template for safety)
       - This is now a simple page (no getStaticPaths/pagination needed -- all posts on one page)

    3. Delete `src/pages/blog/[...page].astro` (replaced by index.astro)

    4. Create `src/pages/blog/[slug].astro` (REPLACE [...id].astro):
       - Import sanityClient, getPostBySlugQuery, getAllPostsQuery, urlFor, and PortableTextRenderer
       - Import BlogLayout (will be updated in next step)
       - Use getStaticPaths:
         ```ts
         export async function getStaticPaths() {
           const posts = await sanityClient.fetch(getAllPostsQuery)
           return posts.map((post: any) => ({
             params: { slug: post.slug.current },
             props: { post }
           }))
         }
         ```
       - BUT do NOT use BlogLayout as-is. Instead, build the page directly using Layout + components:
         - Import Layout from `../../layouts/Layout.astro`
         - Import BlogPostingSchema, Hero, CTAPrimary, Section, Row, Col, PortableTextRenderer
         - Build page structure matching current BlogLayout but with Sanity data:
           - Layout title={post.title} description={post.seoDescription}
           - BlogPostingSchema with Sanity fields (use seoDescription for description prop)
           - Hero with author and formatted date as subtitle, post.title as title
           - Featured image section (if post.featuredImage exists, use urlFor to get URL)
           - PortableTextRenderer for post.content
           - CTAPrimary at bottom

    5. Delete `src/pages/blog/[...id].astro` (replaced by [slug].astro)

    6. Update `src/components/blocks/blog/PostCards.astro`:
       - The component already works with the transformed data format (has `data` prop with `.data.title`, `.data.pubDate`, etc.)
       - The `id` field maps to slug for URL generation
       - No changes needed IF the transformed data matches the existing shape. Verify the `post.id` usage for link generation matches `slug.current`.

    7. Update `src/layouts/BlogLayout.astro`:
       - This layout is no longer used by blog posts (they use Layout directly now)
       - Keep the file but add a comment that it's preserved for reference
       - OR: Delete it if no other pages use it. Check if any non-blog pages import BlogLayout. If none, delete it.
  </action>
  <verify>
    Run `npx astro check` to confirm no type errors.
    Run `npx astro build 2>&1 | tail -30` -- build may fail if Sanity has no content yet, which is expected. Check that the error is about empty query results, not about import/type/syntax issues.
    If build fails due to empty Sanity dataset, that's OK -- Plan 03 seeds content.
  </verify>
  <done>
    Blog listing page imports and uses Sanity client + GROQ queries. Blog post page uses getStaticPaths with Sanity slugs, renders Portable Text via PortableTextRenderer. Old [...page].astro and [...id].astro deleted. PostCards works with transformed Sanity data shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update BlogPostingSchema and remove markdown blog infrastructure</name>
  <files>
    src/components/seo/BlogPostingSchema.astro
    src/content.config.ts
    src/content/blog/
  </files>
  <action>
    1. Update `src/components/seo/BlogPostingSchema.astro`:
       - The component already accepts `description` as a prop
       - The Sanity schema has `seoDescription` field which maps to this prop
       - The blog post page ([slug].astro) passes `post.seoDescription` as `description`
       - If seoDescription is empty/null, use a fallback: truncate post title + " - PromptMarketing Blog"
       - Update the Props interface to make `description` optional with string type:
         ```ts
         interface Props {
           title: string;
           description?: string;
           pubDate: Date | string;
           author: string;
           image?: string;
         }
         ```
       - Handle pubDate being either a Date object or ISO string (Sanity returns strings):
         ```ts
         const dateObj = pubDate instanceof Date ? pubDate : new Date(pubDate);
         ```
       - Use `dateObj.toISOString()` in the schema output
       - For description, use: `const desc = description || title + ' - PromptMarketing Blog'`
       - The author field no longer needs slug-to-name formatting since Sanity stores plain text names (per locked decision). Remove the split/map/join logic. Use author directly.

    2. Update `src/content.config.ts`:
       - Remove the `blog` collection entirely
       - Keep the `portfolio` collection exactly as-is
       - Remove the glob import if portfolio still needs it (it does -- keep glob import)
       - Final file should only export `{ portfolio }` in collections

    3. Delete all files in `src/content/blog/`:
       - Delete `src/content/blog/agentic-commerce.md`
       - Delete `src/content/blog/ai-search-visibility.md`
       - Delete `src/content/blog/founders-perspective.md`
       - Delete `src/content/blog/legible-lovable-standard.md`
       - Delete `src/content/blog/why-rankings-no-longer-matter.md`
       - These are placeholder posts per locked decision ("Existing markdown posts are placeholders, not real content")
       - Remove the `src/content/blog/` directory after deleting files

    4. Verify portfolio still works:
       - Run `npx astro build` and confirm portfolio pages still generate
       - The portfolio collection in content.config.ts must remain untouched
  </action>
  <verify>
    Run `npx astro check` to confirm no type errors.
    Confirm `src/content/blog/` directory no longer exists.
    Confirm `src/content.config.ts` still exports portfolio collection.
    Run `npx astro build 2>&1 | grep -i "portfolio"` to verify portfolio pages still build.
  </verify>
  <done>
    BlogPostingSchema handles optional description with fallback, accepts string dates from Sanity, uses plain author name (no slug formatting). Blog markdown collection removed from content.config.ts. All placeholder markdown files deleted. Portfolio collection untouched and building.
  </done>
</task>

</tasks>

<verification>
- Blog listing page at /blog fetches from Sanity via GROQ
- Blog post page at /blog/[slug] renders Portable Text content
- BlogPostingSchema produces valid JSON-LD with Sanity data
- No markdown blog files remain
- Portfolio collection and pages still build correctly
- `npx astro build` succeeds (with Sanity content seeded in Plan 03)
</verification>

<success_criteria>
Blog pages fully powered by Sanity CMS. Portable Text renders correctly. BlogPostingSchema works with Sanity data including fallback for missing seoDescription. Markdown blog infrastructure completely removed. Portfolio unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/05-sanity-cms-integration/05-02-SUMMARY.md`
</output>
